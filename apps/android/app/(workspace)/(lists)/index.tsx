import { WorkspaceListRoute } from "../../../src/routeComposition/WorkspaceListRoute";

/** Renders the root catalog in the same retained pane at every width. */
export default function V1AllRoute(): React.JSX.Element | null {
  return <WorkspaceListRoute />;
}
