// Metro selects `.native` on Android and `.web` in the browser. Tooling and
// platform-neutral tests use this fallback without loading React Native events.
export * from "./globalVoiceOverlayActions.web";
