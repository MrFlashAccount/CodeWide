# V1 drawing

DrawingFeature owns drawing/annotation sessions and fullscreen presentation; drawingAttachment converts PNG output and retains the opening composer's admission. DrawingWorkspace owns bridge readiness and pending/error/accepted-close behavior. The existing quickdraw data helpers and native image/file adapters remain authoritative.

The session captures its composer scope and admission before asynchronous image loading. A stale image target still fails qualification. PNG replacement preserves the draft card identity; staging acceptance, rather than network completion, closes the editor. Rejected admission or exceptions retain the editor for retry.

M4 drawing: V1 gate passed; 47 focused checks and two render cases passed. Native WebView drawing, exported image appearance and device gesture behavior remain unverified.
