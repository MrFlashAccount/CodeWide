# Manual QA — 2026-08-11

Live checklist for the Android `0.1.7 (10)` hand-test. Keep symptoms, verified causes, fixes, and release evidence together.

| ID | State | Area | Report / expected behavior | Evidence or cause |
| --- | --- | --- | --- | --- |
| QA-01 | fixed-awaiting-device | Images | Opening a thread image reports `Image preview failed`; authenticated inline and fullscreen preview must work. | Installed unit now read-only binds the attachment tree into its `PrivateTmp`; process namespace sees real attachment files. |
| QA-02 | fixed-awaiting-device | Activity | Completed-turn activity is absent after expanding it; it must lazy-load and render chronologically. | Current Codex rejects `thread/items/list` with `-32601`; client now falls back to paginated `thread/turns/list` with `itemsView: full`. |
| QA-03 | fixed-awaiting-device | Activity | Expanding activity produces a one-frame content jump. | LegendList keeps data-prepend anchoring but no longer counter-scrolls on existing-row size changes. |
| QA-04 | fixed-awaiting-device | Bubbles | Codex bubbles must be left-aligned; user bubbles right-aligned. | Explicit left/right child alignment is independent of the full-width context-menu wrapper. |
| QA-05 | fixed-awaiting-device | Bubbles | User bubbles are too narrow/tight while Codex bubbles are visually oversized. | User max width/padding increased; Codex no longer stretches to the full row. |
| QA-06 | fixed-awaiting-device | Time | Message time must use the Android device locale and timezone. | All thread/turn clocks route through the device-default locale and timezone formatter. |
| QA-07 | fixed-new-turns | Usage | Completed footer does not show input/output tokens or estimated cost. | Rust companion persists the live usage projection and reconstructs completed-turn usage/cost from canonical rollout `token_count` records. Android only formats the ready projection; unknown model prices stay unavailable. |
| QA-08 | fixed-awaiting-device | User message | Collapse long user requests after about 25 rendered lines with an explicit expand/collapse action. | Per-message persisted expand/collapse preserves attachment tiles above the text. |
| QA-09 | fixed-awaiting-device | Pin | Pin control must visibly switch between pinned and unpinned states. | Filled/outline pin plus explicit pinned/not-pinned labels. |
| QA-10 | fixed-and-smoked | Tunnel | Tunnel failure must be contained and rendered, not disconnect the server. Improve the target input beyond a bare port. | Installed companion returns `502 localhost_unavailable` and remains healthy; UI accepts a port, `localhost:port`, or loopback URL and shows TTL presets. |
| QA-11 | fixed-awaiting-device | Queue | Queued prompts must appear as an attached panel above the composer, not only in the menu. | Inline deduplicated queue tray opens the existing editor. |
| QA-12 | fixed-awaiting-device | Skills | Tapping a skill should insert its invocation into the draft at the cursor; selection checkmarks are misleading. | Skill tap inserts `$skill-name` at the current cursor and closes the sheet. |
| QA-13 | fixed-awaiting-device | Composer | Show current model, reasoning effort, and permissions as compact chips next to the message field. | Chips use authoritative resumed thread settings unless the user explicitly overrides the next turn. Old auto-selected preferences are cleared once. |
| QA-14 | fixed-awaiting-device | Keyboard | Chat and sheet scroll areas must resize to the visual viewport when the keyboard opens. | Chat now uses a height-resizing native keyboard-controller view; native Material sheets retain platform IME handling. |
| QA-15 | pass | Navigation | Thread navigation is now fast and pleasant. | Confirmed by manual QA on `0.1.7 (10)`. |
| QA-16 | fixed-awaiting-device | Navigation | Switching servers must not leave the previous thread composer interactive or save its draft under the wrong thread. | Server and first-thread selection now commit atomically; warm-navigation draft regression passes. |
| QA-17 | fixed-and-smoked | Voice | Voice fails with `realtime conversation requires API key auth` despite a valid ChatGPT Codex login. | Android now streams PCM only to the companion; Stop/Send assembles WAV and uses the host's mode-0600 Codex OAuth through the ChatGPT Desktop transcription endpoint. OAuth never enters the device protocol, argv, environment, or logs. Live synthetic-WAV smoke returned HTTP 200/transcript; OAuth/PCM/RPC regressions and the full 216-test suite pass. |
| QA-18 | fixed-and-smoked | Streaming | Delta events arrive much slower than Codex emits them; buffer a burst and flush it in one pass. | Companion serialized every event behind its own open/write/fsync/close. It now collects up to 32 ms/256 events, performs one journal write/fsync, then emits compatible `event` frames back-to-back; Android's existing 50 ms render buffer reduces the burst once. |
